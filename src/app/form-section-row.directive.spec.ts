import { FormSectionRowDirective } from './form-section-row.directive';

describe('FormSectionRowDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSectionRowDirective();
    expect(directive).toBeTruthy();
  });
});
