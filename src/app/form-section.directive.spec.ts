import { FormSectionDirective } from './form-section.directive';

describe('FormSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSectionDirective();
    expect(directive).toBeTruthy();
  });
});
