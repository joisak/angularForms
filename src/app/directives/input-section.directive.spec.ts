import { InputSectionDirective } from './input-section.directive';

describe('InputSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSectionDirective();
    expect(directive).toBeTruthy();
  });
});
