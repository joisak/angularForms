import { InputFullDirective } from './input-full.directive';

describe('InputFullDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFullDirective();
    expect(directive).toBeTruthy();
  });
});
