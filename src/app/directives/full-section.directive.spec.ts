import { FullSectionDirective } from './full-section.directive';

describe('FullSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new FullSectionDirective();
    expect(directive).toBeTruthy();
  });
});
