import { QuarterInputDirective } from './quarter-input.directive';

describe('QuarterInputDirective', () => {
  it('should create an instance', () => {
    const directive = new QuarterInputDirective();
    expect(directive).toBeTruthy();
  });
});
