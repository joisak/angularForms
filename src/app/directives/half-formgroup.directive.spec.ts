import { HalfFormgroupDirective } from './half-formgroup.directive';

describe('HalfFormgroupDirective', () => {
  it('should create an instance', () => {
    const directive = new HalfFormgroupDirective();
    expect(directive).toBeTruthy();
  });
});
